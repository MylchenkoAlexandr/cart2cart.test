import React from "react";
import Home from "../Home";

export default function Sign( props ) {
    return (
        <Home>
            <div className="Sign">
                Sign
            </div>
        </Home>
    );
}
