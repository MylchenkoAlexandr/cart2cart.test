import React from "react";

export default function Sign( props ) {
    /* debug */ console.log( "Sign([ props ])", props );
    return (
        <div className="Sign">
            Sign
        </div>
    );
}
