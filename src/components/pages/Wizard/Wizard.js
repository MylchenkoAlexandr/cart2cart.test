import React from "react";

export default function Wizard( props ) {
    /* debug */ console.log( "Wizard([ props ])", props );
    return (
        <div className="Wizard">
            Wizard
        </div>
    );
}
